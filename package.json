{
  "name": "homeassistant-addon-wnsm-sync",
  "version": "1.0.0",
  "description": "This Home Assistant add-on fetches 15-minute interval consumption data from the Wiener Netze Smart Meter portal and injects it into Home Assistant's long-term statistics.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync-changelog": "cp CHANGELOG.md wnsm_sync/CHANGELOG.md",
    "build-addon": "npm run sync-changelog && docker build -t wnsm_sync:latest wnsm_sync/",
    "release": "standard-version",
    "release:full": "npm run release && git push --follow-tags",
    "release:build": "npm run release:full && npm run build-addon"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZakiZtraki/homeassistant-addon-wnsm-sync.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ZakiZtraki/homeassistant-addon-wnsm-sync/issues"
  },
  "homepage": "https://github.com/ZakiZtraki/homeassistant-addon-wnsm-sync#readme",
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "standard-version": {
  "changelogFile": "./wnsm_sync/CHANGELOG.md"
  }
}
